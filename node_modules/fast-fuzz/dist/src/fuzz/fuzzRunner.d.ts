import { Results } from './result';
export declare class FuzzRunner {
    private lock;
    private methodIndexPre;
    private methodIndexPost;
    private pCount;
    private workers;
    init(folder: string, src?: string, dist?: string, instances?: any, pCount?: number): Promise<void>;
    count(methodPattern?: string, classPattern?: string, filePattern?: string): Promise<number>;
    fuzz(maxTime?: number, methodPattern?: string, classPattern?: string, filePattern?: string, resultsOut?: Results[]): Promise<Results[]>;
    getInstances(): Promise<any>;
    terminate(): void;
    private runWorker;
}
