import { ModuleType } from "../utils/modules";
import { Generator } from "./Generator";
export declare class GeneratorEnum extends Generator {
    private type;
    private renderer;
    private isInit;
    constructor(type: ModuleType, dimension?: number, index?: number);
    generate(count: number): any[];
    next(): any;
    private init;
}
