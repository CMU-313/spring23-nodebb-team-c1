import { Generator } from "./Generator";
export declare class GeneratorBool extends Generator {
    constructor(dimension?: number, index?: number);
    generate(count: number): boolean[];
    next(): any;
}
