import { BuiltIn } from "../utils/decorators";
import { ModuleType } from "../utils/modules";
import { IGenerator } from "./IGenerator";
import { Mode } from "./Mode";
export declare class GeneratorFactory {
    static init(type: BuiltIn, dimension: number, literals: string[], index?: number, min?: number, max?: number): IGenerator;
    static initType(type: ModuleType, dimension?: number, index?: number, mode?: Mode, isIgnoreFalsy?: boolean): IGenerator;
}
