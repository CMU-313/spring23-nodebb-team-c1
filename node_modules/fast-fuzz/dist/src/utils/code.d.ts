import { ModuleMethod, ModuleType } from './modules';
export declare class Code {
    private arg;
    private tsFiles;
    private iFiles;
    private jsFiles;
    private typeDefs;
    methods: {
        [key: string]: ModuleMethod[];
    };
    types: {
        [key: string]: ModuleType[];
    };
    interfaces: {
        [key: string]: [string, string];
    };
    modules: {
        [key: string]: any;
    };
    init(tsFiles: string, jsFiles: string): Promise<void>;
    load(): Promise<void>;
    findType(typeName: string, ICode: [string, string], file: string): ModuleType;
    private getJsFile;
    private static getFiles;
    static getLiterals(fileName: any, moduleMethods: ModuleMethod[]): void;
    private static getMethods;
    private getTypes;
    private static getExtends;
    private static getInterfaceFile;
    static getEnumStatements(code: string): string;
    private static getMethodArguments;
}
