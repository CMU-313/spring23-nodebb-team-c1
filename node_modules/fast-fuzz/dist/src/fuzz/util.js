"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.simpleHash = exports.resetCoverage = exports.cleanupError = void 0;
function cleanupError(error) {
    const stack = error.stack.split('\n');
    stack.splice(-8);
    error.stack = stack.join('\n');
}
exports.cleanupError = cleanupError;
function resetCoverage(fileName) {
    const fileCov = global.__coverage__[fileName];
    fileCov.s = {};
    for (const key in fileCov.b) {
        const branchCov = fileCov.b[key];
        for (const index in branchCov) {
            branchCov[index] = 0;
        }
    }
    for (const key in fileCov.bT) {
        const branchCov = fileCov.bT[key];
        for (const index in branchCov) {
            branchCov[index] = 0;
        }
    }
    return true;
}
exports.resetCoverage = resetCoverage;
function simpleHash(str) {
    let hash = 0;
    for (let i = 0; i < str.length; i++) {
        const char = str.charCodeAt(i);
        hash = (hash << 5) - hash + char;
        hash &= hash;
    }
    return new Uint32Array([hash])[0].toString(36);
}
exports.simpleHash = simpleHash;
