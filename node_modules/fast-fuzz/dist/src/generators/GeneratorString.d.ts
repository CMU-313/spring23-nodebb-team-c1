import { Generator } from "./Generator";
export declare class GeneratorString extends Generator {
    private static MODE_SCALE;
    constructor(dimension: number, literals: string[], min?: number, max?: number, index?: number);
    generate(count: number): string[];
    next(): any;
    private static getLimits;
}
