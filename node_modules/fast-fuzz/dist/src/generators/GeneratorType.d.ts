import { ModuleType } from "../utils/modules";
import { Generator } from "./Generator";
import { Mode } from "./Mode";
export declare class GeneratorType extends Generator {
    private static interfaces;
    private typeArgs;
    private types;
    private numTypes?;
    private mode;
    private isIgnoreFalsy;
    private nextCount?;
    private instanceTypes?;
    private typesCount?;
    constructor(type: ModuleType, dimension?: number, index?: number, mode?: Mode, isIgnoreFalsy?: boolean);
    generate(count: number): any[];
    next(): any;
    nextTypes(): {
        index: number;
        dimension: number;
        types: ModuleType[];
    };
    private init;
    private generateStuffInstances;
    private generateSingle;
    private generateMany;
    private static shuffle;
}
