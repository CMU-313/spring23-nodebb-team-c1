import { Generator } from "./Generator";
export declare class GeneratorArg extends Generator {
    private interfaces;
    constructor(interfaces: [string, string][]);
    generate(count: number): any[];
    next(): any;
}
